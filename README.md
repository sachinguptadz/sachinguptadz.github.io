# Sachin Gupta Premium React Portfolio

## Local run
```bash
npm install
npm run dev
```

## Build for GitHub Pages
```bash
npm run build
```

This project uses Vite with `base: './'`, so it works on GitHub Pages project URLs.
